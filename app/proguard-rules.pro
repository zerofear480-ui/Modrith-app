# Project-specific rules are intentionally empty. Shipped AndroidX, Hilt,
# Room, Kotlin serialization, OkHttp, and Compose dependencies provide their
# required consumer rules. Release builds use the optimized Android defaults.
